
let product_data = {
    id: csv_headers["id"],
    name : csv_headers["name"],
    unit_price : csv_headers["unit_price"],
    discount: csv_headers["discount"],
    shipping_cost: csv_headers["shipping_cost"]
}

module.exports = {product_data}