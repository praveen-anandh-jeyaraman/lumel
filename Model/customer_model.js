let customer_data = {
    id: csv_headers["customer_data"],
    name : csv_headers["name"],
    email : csv_headers["email"],
    address: csv_headers["address"],
    shipping_cost: csv_headers["shipping_cost"]
}

module.exports = {customer_data}